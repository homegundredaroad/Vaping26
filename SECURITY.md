# Security

Do not report exposed credentials or sensitive unpublished evidence in a public issue.

This repository must never contain API keys, passwords, private SSH keys, access tokens, raw protected investigative material or personal information that is not appropriate for publication.

If a credential is accidentally committed, revoke or rotate it immediately; deleting it from the latest commit is not sufficient.
